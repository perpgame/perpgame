services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: hltrader
      POSTGRES_PASSWORD: hltrader
      POSTGRES_DB: hltrader
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
